services:
  bot-tele:
    image: ikhsan123/bot-tele:latest
    environment:
      APP_ENV: production

  bot-wa:
    image: ikhsan123/bot-wa:latest
    environment:
      APP_ENV: production

  core-api:
    image: ikhsan123/core-api:latest
    environment:
      APP_ENV: production
    ports: [] # No exposed ports in production

  gateway:
    image: ikhsan123/gateway:latest

  landing-page:
    image: ikhsan123/landing-page:latest